# 文本撰寫與 Dialogue 格式規範

本專案使用 Godot 的 `dialogue_manager` 插件（.dialogue 格式）來管理對話文本。以下為後續撰寫與修改對話時必須遵守的規則：

## 1. 基礎語法結構

### 節點定義 (Nodes)
* 每個對話段落/事件以 `~ 節點名稱` 開頭。
* 範例：
  ```dialogue
  ~ leave_room
  ```

### 角色發言與旁白 (Speakers & Narration)
* **`It: `**：代表旁白、第三人稱動作描述、聲音特效或系統提示。
  * 範例：`It: 浴室裡持續傳來嘩啦啦的水聲。`
* **`Me: `**：代表主角（攝影師）的內心獨白或發言。
  * 範例：`Me: 還是等她洗完出來再說吧。`
* **`Keiko: `**：代表女主角「惠子」的發言。
* **`Client: `**：代表委託人/客戶的發言。
* 所有對話行格式為：`角色名稱: 對話內容`。

### 選項與分支 (Options & Branches)
* 使用減號 `-` 開頭表示選項，選項內容後縮排撰寫該選項觸發的對話或行為。
* 範例：
  ```dialogue
  ~ check_camera
  It: 要現在架設相機嗎？
  - 架設相機
      It: 我撐開腳架，將相機固定在房間中央。
      => END
  - 晚點再架設
      => END
  ```

### 流程控制 (Flow Control)
* **`=> END`**：結束當前對話。
* **`=> 節點名稱`**：跳轉至指定的對話節點。

---

## 2. 角色設定與語氣規範

* **主角 (Me)**：
  * 職業為攝影師。
  * 個性較為務實、謹慎，帶有一點愛嘮叨的「媽媽」屬性。
  * 對惠子的生活習慣和特徵非常熟悉（例如知道她哪邊臉上相、腰圍尺寸）。
* **惠子 (Keiko)**：
  * 擔任模特兒。
  * 個性隨興、有點懶散（房間很亂）、愛撒嬌和開玩笑。
  * 喜歡對主角開玩笑或提出一些任性的要求（例如要求改短裙子、換領結顏色）。
* **語系**：
  * 統一使用 **繁體中文 (zh-TW)**。
  * 語氣要自然、口語化，符合現代日常生活對話的調性。

---

## 3. 專案開發整合規範

* **檔案管理與路徑**：
  * 新增或修改對話時，統一沿用 `addons/dialogue_manager` 插件資源，並放置在 `dialogue/` 與 `translation/` 的既有流程中。
* **事件整合**：
  * 關卡開場或互動點觸發的對話，需配合 `EventTree` + `DialogueEvent` 的事件元件來播放，避免直接在玩家或關卡基底硬編碼對話邏輯，確保 `Game.game_state` 狀態切換正常。

